# Binary circuit data (simplified representation)
# In production this would be actual compiled circuit bytecode

# Circuit metadata
MAGIC: 0x4D545020  # "MTP "
VERSION: 1
CONSTRAINTS: 8
VARIABLES: 16
PUBLIC_INPUTS: 8

# Constraint data (simplified)
# Each constraint: left_var * right_var = output_var
00 01 08  # bounds_min[0] * 1 = temp[0]
09 01 10  # traffic_stats[0] * 1 = temp[1] 
10 0F 11  # temp[1] - temp[0] = diff[0] (must be >= 0)

01 02 08  # bounds_min[1] * 1 = temp[0]
0A 01 10  # traffic_stats[1] * 1 = temp[1]
10 0F 11  # temp[1] - temp[0] = diff[1]

02 03 08  # bounds_min[2] * 1 = temp[0] 
0B 01 10  # traffic_stats[2] * 1 = temp[1]
10 0F 11  # temp[1] - temp[0] = diff[2]

03 04 08  # bounds_min[3] * 1 = temp[0]
0C 01 10  # traffic_stats[3] * 1 = temp[1] 
10 0F 11  # temp[1] - temp[0] = diff[3]

# Similar constraints for upper bounds...
# Randomness constraint for zero-knowledge
0D 0D 0E  # randomness * randomness = randomness_squared

# End marker
FF FF FF